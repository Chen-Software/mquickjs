// Generated by `wit-bindgen` 0.55.0. DO NOT EDIT!
#ifndef __BINDINGS_MICROQUICKJS_H
#define __BINDINGS_MICROQUICKJS_H
#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct microquickjs_string_t {
  uint8_t*ptr;
  size_t len;
} microquickjs_string_t;

// Exported Functions from `microquickjs`
void exports_microquickjs_eval(microquickjs_string_t *code, microquickjs_string_t *ret);

// Helper Functions

// Sets the string `ret` to reference the input string `s` without copying it
void microquickjs_string_set(microquickjs_string_t *ret, const char*s);

// Creates a copy of the input nul-terminated string `s` and
// stores it into the component model string `ret`.
void microquickjs_string_dup(microquickjs_string_t *ret, const char*s);

// Creates a copy of the input string `s` with length `len` and
// stores it into the component model string `ret`.
// The length is specified in code units (bytes for UTF-8, 16-bit values for UTF-16).
void microquickjs_string_dup_n(microquickjs_string_t *ret, const char*s, size_t len);

// Deallocates the string pointed to by `ret`, deallocating
// the memory behind the string.
void microquickjs_string_free(microquickjs_string_t *ret);

#ifdef __cplusplus
}
#endif
#endif
